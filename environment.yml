name: uav-fgs
channels:
  - pytorch
  - nvidia
  - defaults
dependencies:
  - python=3.10.18
  - pip=25.2
  - numpy=1.26.4
  - pytorch=2.0.1
  - torchvision=0.15.2
  - pytorch-cuda=11.8
  - pip:
      - -r requirements.txt
